# Fluid Flow Engineering Dashboard

This Streamlit app calculates pipe-flow velocity, Reynolds number, friction factor and pressure drop using the Darcy-Weisbach equation. The user enters the fluid and pipe data through the sidebar. The app displays the calculated results in a table and shows how pressure drop changes with flow rate on a Plotly chart. Invalid inputs are handled with a warning.

Live app URL: To be added after deployment.
